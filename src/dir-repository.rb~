# -*- coding: utf-8 -*-
class DirRepsitory
  # 仕様を見てコードを追加
  attr_reader :dir_name, :header, :type, :format, :max

  # 仕様を見てコードを追加
  def initialize(dir_name, header)
    @dir_name = dir_name
    @header = header
    @type = type
    @format = format
    @max = max
  end

  # メソッドの中身のコードを追加
  def make
  end
end
